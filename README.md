# Windows Server & Client application for transmitting data<br />
Access via console (which is automatically generated if not launched throught cmd)<br />
Usage:
1) Launch the server (no parameters for now)<br />
2) Launch the client (1 parameter - ip, defaults to localhost)<br />

(Work in progress)
